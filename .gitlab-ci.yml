stages:
  - apps
  - realms

build-apps:
  stage: apps
  trigger:
    include: apps/.gitlab-ci.yml
    strategy: depend
  rules:
    - changes:
      - apps/**/*

realms:
  stage: realms
  trigger:
    include: realms/.gitlab-ci.yml
    strategy: depend
  rules:
    - changes: 
      - realms/**/*
