# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: realms.sso.kacd.kz
spec:
  group: sso.kacd.kz
  names:
    kind: Realm
    plural: realms
    shortNames:
    - rlm
    singular: realm
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              displayedName:
                description: Displayed name of the realm
                type: string
              frontendUrl:
                description: Host for specific realm
                type: string
              requireSsl:
                description: Mode of the ssl requirement
                type: string
              login:
                description: configuration of the login
                properties:
                  loginScreen:
                    description: Login screen config
                    properties:
                      registration:
                        description: Is user registration available
                        type: boolean
                      forgotPassword:
                        description: Is forgot password available
                        type: boolean
                      rememberMe:
                        description: Is remember me available
                        type: boolean
                    type: object
                  email:
                    description: email settings
                    properties:
                      emailAsUsername:
                        description: Is email is a valid username
                        type: boolean
                      loginWithEmail:
                        description: Are users allowed to log in with email
                        type: boolean
                      duplicatesEmails:
                        description: Is user's emails can be duplicated
                        type: boolean
                      verifyEmail:
                        description: Are users forced to verify their emails
                        type: boolean
                    type: object
                  info:
                    description: User info settings
                    properties:
                      editUsername:
                        description: Are users allowed to edit usernames
                        type: boolean
                    type: object
                type: object
              email:
                description: Email options
                properties:
                  from:
                    description: Email address of the sender
                    type: string
                  smtpHost:
                    description: Host of smtp server
                    type: string
                  smtpPort:
                    description: Port of smtp server
                    type: integer
                  encryption:
                    description: Mode of encryption
                    items:
                      type: string
                    type: array
                  authentication:
                    description: authentication settings
                    properties:
                      existingSecret:
                        description: Name of the secret with authentication credentials
                        type: string
                      usernameKey:
                        description: Name of the key where username is stored
                        type: string
                      passwordKey:
                        description: Name of the key where password is stored
                        type: string
                    type: object
                type: object
              themes:
                description: Specify themes for realm
                properties:
                  login:
                    description: Theme of login page
                    type: string
                  account:
                    description: Theme of the account page
                    type: string
                  admin:
                    description: Theme of the admin console page
                    type: string
                  email:
                    description: Theme of the email
                    type: string
                type: object
              events:
                description: Configuration of user events storing
                properties:
                  eventListeners:
                    description: List of event listeners
                    items:
                      type: string
                    type: array
                  userEvents:
                    description: Config of user events
                    properties:
                      saveEvents:
                        description: If true user events will be enabled
                        type: boolean
                      expiration:
                        description: "Duration of user events persist time (duration\
                          \ can be m(Minutest), h(Hours), d(Days))"
                        type: string
                      savedTypes:
                        description: "List of event types which will be saved. If\
                          \ empty and events active, it creates standard list of types."
                        items:
                          type: string
                        type: array
                    type: object
                  adminEvents:
                    description: Config of admin events
                    properties:
                      saveEvents:
                        description: If true admin events will be stored
                        type: boolean
                      includeRepresentation:
                        description: If true admin events will be stored with it's
                          representations
                        type: boolean
                      expiration:
                        description: "expiration of the admin events (duration can\
                          \ be m(Minutest), h(Hours), d(Days))"
                        type: string
                    type: object
                type: object
              localization:
                description: Localization settings
                properties:
                  i17n:
                    description: If true internationalization will be enabled
                    type: boolean
                  supportedLocales:
                    description: List of supported locales
                    items:
                      type: string
                    type: array
                  defaultLocale:
                    description: Default locale if no one is defined
                    type: string
                type: object
              security:
                description: Config of additional security
                properties:
                  bruteForce:
                    description: Brute force detection config
                    properties:
                      enabled:
                        description: If true Brute force attempts will be detected
                        type: boolean
                      maxLoginFailures:
                        description: Count of login failures before lockout
                        type: integer
                      permanentLockout:
                        description: If true after brute force detection user will
                          be locked permanently
                        type: boolean
                      waitIncrement:
                        description: "How much time user should be locked out (duration\
                          \ can be m(Minutest), h(Hours), d(Days))"
                        type: string
                      maxWait:
                        description: "Maximum time of user lockout (duration can be\
                          \ m(Minutest), h(Hours), d(Days))"
                        type: string
                      failureResetTime:
                        description: "Time after which failure count will be reset\
                          \ (duration can be m(Minutest), h(Hours), d(Days))"
                        type: string
                      quickLoginMillis:
                        description: If login failures happens too quickly user will
                          be locked
                        type: integer
                      quickLoginWait:
                        description: "How long to wait after a quick login failure\
                          \ (duration can be m(Minutest), h(Hours), d(Days))"
                        type: string
                    type: object
                type: object
              sessions:
                description: Sessions config
                properties:
                  sessionIdle:
                    description: Session expiration if no refresh actions requested
                    type: string
                  sessionMax:
                    description: Maximum lifetime of the session
                    type: string
                  offlineSessionIdle:
                    description: Offline session lifetime when no refresh actions
                      requested
                    type: string
                type: object
              tokens:
                description: Settings of tokens
                properties:
                  accessLifespan:
                    description: "lifetime of the access token (It is recommended\
                      \ for this value to be shorter than the SSO session idle timeout:\
                      \ 30 minutes)"
                    type: string
                  oidcAccessLifespan:
                    description: OIDC implicid flow access token lifetime
                    type: string
                type: object
              federations:
                description: Settings of user federations
                properties:
                  ldap:
                    description: Configuration of ldap federation
                    properties:
                      displayedName:
                        description: Displayed name in admin console
                        type: string
                      vendor:
                        description: Name of the vendor of ldap users
                        enum:
                        - ACTIVE_DIRECTORY
                        - code
                        type: string
                      connection:
                        description: Ldap connection settings
                        properties:
                          url:
                            description: Full specified url to connect to ldap. E.g.
                              ldaps://mydomain:689
                            type: string
                          startTls:
                            description: If true start TLS will be used to encrypt
                              messages
                            type: boolean
                          useTrustStore:
                            description: Mode of using Java trust store. LDAPS_ONLY
                              is default and means that only with ldaps protocol trust
                              store will be used. ALWAYS means that every protocols
                              will be used with trust store. NONE means that trust
                              store will not be used at all.
                            enum:
                            - LDAPS_ONLY
                            - ALWAYS
                            - NEVER
                            - code
                            type: string
                          connectionPooling:
                            description: If true connection pooling will be used to
                              access LDAP server
                            type: boolean
                          connectionTimeout:
                            description: "Duration of connection timeout (duration\
                              \ can be s(Seconds) m(Minutest), h(Hours), d(Days))"
                            type: string
                          auth:
                            description: Authentication properties of LDAP admin
                            properties:
                              type:
                                description: Type of credentials binding. By default
                                  this is simple binding.
                                enum:
                                - SIMPLE
                                - NONE
                                - code
                                type: string
                              existingSecret:
                                description: Name of the existing simple which contains
                                  auth DN and password
                                type: string
                              dnKey:
                                description: Key of the authentication DN inside existing
                                  secret
                                type: string
                              passwordKey:
                                description: Key of the authentication password inide
                                  existing secret
                                type: string
                            type: object
                          searching:
                            description: Properties of searching users in LDAP
                            properties:
                              mode:
                                description: Mode of access users in LDAP. By default
                                  is R - read only access. RW - read and write access;
                                  allows admins edit users using keycloak admin console.
                                enum:
                                - R
                                - RW
                                - code
                                type: string
                              usersDn:
                                description: Describes full DN of UO where all users
                                  is stored.
                                type: string
                              username:
                                description: Describes name of the username attribute
                                type: string
                              uuid:
                                description: Describes name of the UUID attribute
                                type: string
                              classes:
                                description: "List comma and space separated of user\
                                  \ classes. By default person, organizationalPerson,\
                                  \ user"
                                type: string
                              filter:
                                description: Additional LDAP filter on users
                                type: string
                              scope:
                                description: Scope of the searching. By default it
                                  is SUBTREE - search all sub UO for users. FLAT -
                                  search only in the first level of UO.
                                enum:
                                - SUBTREE
                                - FLAT
                                - code
                                type: string
                              readTimeout:
                                description: "Timeout of the read of users (duration\
                                  \ can be s(Seconds) m(Minutest), h(Hours), d(Days))"
                                type: string
                              pagination:
                                description: If true keycloak will be used pagination.
                                  Make sure that your LDAP support this function
                                type: boolean
                            type: object
                          sync:
                            description: Synchronizing settings
                            properties:
                              importUsers:
                                description: If true users will be imported into keycloak
                                  DB
                                type: boolean
                              syncRegistrations:
                                description: If true all registrations will be persisted
                                  in LDAP
                                type: boolean
                              batch:
                                description: Size of one transaction batch of users
                                type: integer
                              fullSync:
                                description: If true full sync will be periodically
                                  ran
                                type: boolean
                              fullSyncPeriod:
                                description: "Period of full sync (duration can be\
                                  \ s(Seconds) m(Minutest), h(Hours), d(Days))"
                                type: string
                              changedSync:
                                description: If true changed users will be synced
                                  periodically
                                type: boolean
                              changedSyncPeriod:
                                description: "Period of changed sync (duration can\
                                  \ be s(Seconds) m(Minutest), h(Hours), d(Days))"
                                type: string
                            type: object
                          cache:
                            description: Cache settings
                            properties:
                              policy:
                                description: Mode of the cache. By default it is DEFAULT
                                  - global cache settings. EVICT_DAILY - every day
                                  cache will be evicted. EVICT_WEEKLY - every week
                                  cache will be evicted.
                                enum:
                                - DEFAULT
                                - EVICT_DAILY
                                - EVICT_WEEKLY
                                type: string
                            type: object
                          advanced:
                            description: Advanced settings
                            properties:
                              validatePassword:
                                description: If true keycloak password verification
                                  policy will be applied on ldap users.
                                type: boolean
                              trustEmail:
                                description: If true all emails in ldap will be trusted
                                  by keycloak
                                type: boolean
                            type: object
                          groups:
                            description: Groups mapping settings
                            properties:
                              dn:
                                description: DN of UO where all groups are stored.
                                type: string
                              name:
                                description: Name of the groups name attribute (default
                                  cn)
                                type: string
                              hierarchy:
                                description: If true hierarchy of the groups in ldap
                                  will be saved in keycloak.
                                type: boolean
                              ignoreMissing:
                                description: If true all missing groups in hierarchy
                                  will be ignored
                                type: boolean
                              attributeName:
                                description: Name of the membership attribute in user
                                  representation
                                type: string
                              filter:
                                description: Additional filter of groups in ldap
                                type: string
                              mode:
                                description: Mode of the groups management. By default
                                  R - it means read only mode. RW - all groups mapping
                                  will be retrieved from LDAP and stored in LDAP.
                                  RWC - import groups mapping into keycloak DB and
                                  manages it in DB.
                                enum:
                                - R
                                - RW
                                - RWC
                                - code
                                type: string
                              dropNonExisting:
                                description: If true all non-existing in LDAP groups
                                  will be dropped.
                                type: boolean
                              path:
                                description: Base path of groups hierarchy in keycloak.
                                  By default /
                                type: string
                            type: object
                        type: object
                    type: object
                type: object
            type: object
          status:
            properties:
              state:
                description: State of the realm application
                enum:
                - DETECTED
                - APPLYING
                - APPLIED
                - FAILED
                type: string
              message:
                description: Additional message for state
                type: string
              generation:
                description: Applied generation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
