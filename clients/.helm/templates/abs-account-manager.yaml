apiVersion: sso.kacd.kz/v1
kind: Client
metadata:
  name: abs-account-manager
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: external-realm
    kz-kacd-realm-name: external
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  displayedName: ABS+ Account Manager
  displayedDescription: сервис для ABS+ Account Manager 
  access:
    validRedirectUris:
    - http://localhost:8080/*
    webOrigins:
    - "+"
  capability:
    type: CONFIDENTIAL
    clientExistingSecret: abs-account-manager-secret
    clientSecretKey: client_secret
  roles:
  - name: bank-client-front-direct-holder
    roleDescription: Роль для доступа на просмотр блоков abs+