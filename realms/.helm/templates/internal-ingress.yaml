apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ printf "%s-internal" (include "common.names.fullname" .) }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: keycloak
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  annotations:
    {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
    {{- end }}
    {{- if .Values.internal.ingress.annotations }}
    {{- include "common.tplvalues.render" (dict "value" .Values.internal.ingress.annotations "context" $) | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.internal.ingress.ingressClassName }}
  ingressClassName: {{ .Values.internal.ingress.ingressClassName | quote }}
  {{- end }}
  rules:
    - host: {{ .Values.internal.hostname }}
      http:
        paths:
        - path: /realms/internal
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.internal.ingress.serviceName }}
              port:
                name: http
        - path: /admin/internal/console
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.internal.ingress.serviceName }}
              port:
                name: http
        - path: /resources
          pathType: Prefix
          backend:
            service:
              name: {{ .Values.internal.ingress.serviceName }}
              port:
                name: http
  {{- if and .Values.internal.ingress.tls }}
  tls:
    - hosts:
        - {{ (tpl .Values.internal.hostname .) | quote }}
      secretName: {{ .Values.internal.ingress.secretName }}
  {{- end }}
